<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppFlyoutPage.Menu"
             Title="Menu" BackgroundColor="LightGray">
    <VerticalStackLayout>
        <Label 
            Text="MENU" 
            FontSize="48"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />

        <Button Text="Página 1" BackgroundColor="Transparent" TextColor="Black" Clicked="OnButtonClicked1"/>
        <Button Text="Página 2" BackgroundColor="Transparent" TextColor="Black" Clicked="OnButtonClicked2"/>
        <Button Text="Página 3" BackgroundColor="Transparent" TextColor="Black" Clicked="OnButtonClicked3"/>
    </VerticalStackLayout>
</ContentPage>